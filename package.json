{
  "name": "pact",
  "version": "0.1.0",
  "private": true,
  "description": "Provable agent work settlement and reputation on GIWA",
  "workspaces": [
    "shared",
    "contracts",
    "services/api",
    "services/indexer",
    "giwa"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev -w @pact/api",
    "start": "npm run start -w @pact/api",
    "demo:seed": "npm run demo:seed -w @pact/api",
    "demo:run": "npm run demo:run -w @pact/api",
    "contracts:compile": "npm run build -w @pact/contracts",
    "contracts:deploy:giwa": "npm run deploy:giwa -w @pact/contracts",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
