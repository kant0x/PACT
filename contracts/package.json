{
  "name": "@pact/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/compile.js",
    "test": "npm run build && vitest run",
    "deploy:testnet": "npm run build && node scripts/deploy_testnet.js",
    "deploy:points:testnet": "npm run build && node scripts/deploy_platform_points.js"
  },
  "devDependencies": {
    "ethers": "^6.15.0",
    "ganache": "^7.9.2",
    "solc": "^0.8.30",
    "vitest": "^3.2.4"
  }
}
