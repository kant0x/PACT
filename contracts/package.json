{
  "name": "@pact/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/compile.js",
    "test": "npm run build && vitest run",
    "deploy:giwa": "npm run build && node scripts/deploy_giwa.js"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "ethers": "^6.15.0",
    "ganache": "^7.9.2",
    "solc": "^0.8.30",
    "vitest": "^3.2.4"
  }
}
